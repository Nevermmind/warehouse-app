# 快速添加功能配置说明

## 功能介绍

快速添加功能使用 AI（DeepSeek）来解析自然语言输入，自动提取物品信息：

- ✅ **文字输入**：例如"牛奶，3天后过期"
- ✅ **语音输入**：点击麦克风按钮说话
- ✅ **AI 解析**：自动提取物品名称、分类、过期日期
- ✅ **人工确认**：需要你确认后才添加到数据库

## 配置步骤

### 1. 获取 DeepSeek API Key

1. 访问 [DeepSeek 开放平台](https://platform.deepseek.com/)
2. 注册/登录账号
3. 进入"API Keys"页面
4. 点击"创建 API Key"
5. 复制 API Key

### 2. 配置环境变量

在项目根目录创建 `.env` 文件：

```bash
# 复制示例文件
cp .env.example .env
```

编辑 `.env` 文件，填入你的 API Key：

```env
VITE_DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 3. 重启开发服务器

```bash
# 停止当前服务器（Ctrl+C）
# 重新启动
npm run dev
```

## 使用示例

### 文字输入示例

```
牛奶，3天后过期
添加面包，分类零食，一周后过期
我买了苹果，在水果分类，5天后过期
酸奶 分类饮料 10天后过期
```

### 语音输入示例

点击"🎤 语音输入"按钮，然后说话：
- "牛奶三天后过期"
- "添加面包，分类是零食"
- "苹果，五天后过期"

## 注意事项

1. **浏览器支持**：语音输入功能推荐使用 Chrome 或 Edge 浏览器
2. **API 费用**：DeepSeek API 按使用量计费，价格非常便宜（约 ¥0.001/次）
3. **日期识别**：AI 可以理解相对日期（如"3天后"、"下周"）
4. **分类匹配**：AI 会自动匹配你已有的分类，如果找不到会留空让你选择

## 故障排除

### 问题：点击"解析并继续"没反应

**可能原因**：
- API Key 未配置或配置错误
- 网络问题
- API 额度用完

**解决方法**：
1. 检查浏览器控制台（F12）查看错误信息
2. 确认 `.env` 文件中的 API Key 正确
3. 确认网络可以访问 `api.deepseek.com`

### 问题：语音输入不可用

**可能原因**：浏览器不支持 Web Speech API

**解决方法**：
- 使用 Chrome 或 Edge 浏览器
- 确认已授予麦克风权限

### 问题：解析结果不准确

**解决方法**：
1. 点击"返回修改"调整输入
2. 在确认页面手动修改解析结果
3. 使用更明确的描述，例如：
   - ✅ "牛奶，分类饮料，2024-02-10过期"
   - ❌ "牛奶"

## API 费用参考

DeepSeek 定价（2026年）：
- DeepSeek-Chat：¥1 / 1M tokens
- 一次快速添加约消耗 200-300 tokens
- **大约 1 元钱可以解析 3000-5000 次**

非常便宜！
