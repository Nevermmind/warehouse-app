-- ============================================
-- 添加"中小学生寒暑假"字段
-- ============================================

-- 添加寒暑假标记字段到 commute_records 表
ALTER TABLE commute_records
ADD COLUMN IF NOT EXISTS is_school_holiday BOOLEAN DEFAULT false;

-- ============================================
-- 执行完成后：
-- - 新字段 is_school_holiday 默认为 false
-- - 前端勾选后，根据日期自动判断显示"寒假"（1-3月，蓝色）或"暑假"（6-9月，红色）
-- ============================================
